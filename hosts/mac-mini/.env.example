# Copy to .env and fill in. Never commit .env.
ADMIN_TOKEN=
EDITH_SECRET=
RAVEN_SECRET=
VOICE_SECRET=
ANTHROPIC_API_KEY=
# OPENAI_API_KEY enables the voice node's gpt-realtime-2 sessions.
# Without it, the voice node still registers and the inspector at :8807
# loads, but voice.start_session returns {"error":"openai_key_missing"}.
OPENAI_API_KEY=
CORE_URL=http://100.109.10.50:8000
