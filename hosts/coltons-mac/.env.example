# Copy to .env and fill in. Never commit .env.
CONTROL_SECRET=
CORE_URL=http://100.109.10.50:8000

# Voice node — must match the VOICE_SECRET in mac-mini/.env so Core can verify signatures.
VOICE_SECRET=
OPENAI_API_KEY=
